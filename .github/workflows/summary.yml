            - name: GitHub Actions CD
  # You may pin to the exact commit or the version.
  # uses: GriceTurrble/github-actions-cd-action@fe36ef474ddf82cc2345f56b425b4fe62a194779
  uses: GriceTurrble/github-actions-cd-action@v0.0.2
  with:
    # The token that the action will use to create releases.
    token: # optional, default is ${{ github.token }}
    # Path to a file where the current version is tracked.
The file should contain a semver-compliant version.
Changes to this file trigger the workflow to create releases and tags.

    version_file: # optional, default is VERSION
                      - name: Action release tagger
  # You may pin to the exact commit or the version.
  # uses: tj-actions/release-tagger@1a9264b0fd99a1ef92c4fd2f077f292900cc79b6
  uses: tj-actions/release-tagger@v4.0.0
  with:
    # [GITHUB\_TOKEN](https://docs.github.com/en/free-pro-team@latest/actions/reference/authentication-in-a-workflow#using-the-github_token-in-a-workflow) or a repo scoped [Personal Access Token](https://docs.github.com/en/free-pro-team@latest/github/authenticating-to-github/creating-a-personal-access-token)
    token: # optional, default is ${{ github.token }}
    # Github CLI token
    gh_token: # optional, default is ${{ github.token }}
    # File to write release notes to
    release_notes_file: # default is RELEASE_NOTES.md
          name: Summarize new issues

on:
  issues:
    types: [opened]

jobs:
  summary:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      models: read
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run AI inference
        id: inference
        uses: actions/ai-inference@v1
        with:
          prompt: |
            Summarize the following GitHub issue in one paragraph:
            Title: ${{ github.event.issue.title }}
            Body: ${{ github.event.issue.body }}

      - name: Comment with AI summary
        run: |
          gh issue comment $ISSUE_NUMBER --body '${{ steps.inference.outputs.response }}'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
          RESPONSE: ${{ steps.inference.outputs.response }}
